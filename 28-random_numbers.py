"""
Random Module in Python.

import random


EXAMPLE - Random numbers:

x = random.randint(1, 6) <- Random number between 1 - 6.
y = random.random() <- Random floating number between 0 - 1.


EXAMPLE - Random items:

my_list = ['rock', 'paper', 'scissors']
z = random.choice(my_list) <- Random element in the list.


cards = [1, 2, 3, 4, 5, 6, 7, 8, 9, "J", "Q", "K", "A"]
random.shuffle(cards) <- Shuffles the cards list.


"""