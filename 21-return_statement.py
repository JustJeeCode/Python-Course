"""

Return statement = Functions send Python values/objects back to the caller.
				   These values/objects are known as the function's return value.


SYNTAX:

return x <- 'x' here would be our data or variable.


EXAMPLE:

def multiply(number1, number2):
	return number1 * number2

x = multiply(6, 8) <- x = the return of the multiply function.

We can also assign functions to variables, as python just sees a function
as a variable.

print(x) | OUTPUT => 48

"""