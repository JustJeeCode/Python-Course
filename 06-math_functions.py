"""
MATH FUNCTIONS IN PYTHON:

Here we are importing a module. This allows us to gain functions from 
other files.

We are importing the built-in math module.
vvvvvvvvvvv
import math

We are assigning some variables:

pi = 3.14
x = 1
y = 2
z = 3

MATH FUNCTIONS:

Rounds to whole number:
print(round(pi)) | OUTPUT => 3

Rounds a number up to the nearest whole number:
print(math.ceil(pi)) | OUTPUT => 4

Rounds a number down to the nearest whole number:
print(math.floor(pi) | OUTPUT => 3

Tells you how far a number is from 0:
print(abs(pi)) | OUTPUT => 3.14

Raises a number to the power of x:
print(pow(pi, 2)) | OUTPUT => 9.85

Finds the square root of a number:
print(math.sqrt(420)) | OUTPUT => 20.49

Finds the maximum value out of a collection:
print(max(x, y, z)) | OUTPUT => 3

Finds the minimum value out of a collection:
print(min(x, y, z)) | OUTPUT => 1

"""

